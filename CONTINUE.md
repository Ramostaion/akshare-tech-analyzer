# 项目继续开发记录

## 当前状态

- 项目版本：`0.3.0`
- 项目路径：`C:\Users\MattLiu\Desktop\工具包\akshare-tech-analyzer`
- “可解释、可回测、可统计验证的量化技术信号研究平台”第一版增量改造已经完成。
- 正常启动端口为 `8000`；验证时发现该端口已有平台实例，另用 `8001` 临时测试且不干扰
  现有服务，验证后已关闭临时服务。
- 当前分支为 `main`，HEAD 是 `39eab78`（“威夫科理论”），与 `origin/main` 同步；工作树
  包含本次当前决策、条件计划和历史 Trigger 去重改造，尚未提交。

## 已完成内容

### 0.3.0 量化信号研究平台

- 新增 Factor、Market Regime、四类 Setup 和结构化 TradingSignal 分层。
- 新增下一根执行、费用、滑点、T+1、Triple Barrier、TradeRecord 和完整绩效统计。
- 新增 Factor 分桶、参数扫描、时间序列切分、Walk Forward 与 Regime 分层研究工具。
- 新增基于右侧确认 Pivot、ATR ZigZag 和硬规则的 Elliott Wave Top-N 候选。
- Web API、页面和离线报告向后兼容接入量化结果，规则分与历史概率明确分离。
- README 已记录无未来函数约束及 Stop/Target 同根时按 Stop 处理的保守规则。

### 基础平台

- FastAPI 工作台、JSON API、CLI、SQLite JSON 缓存、统一日志和单文件离线 HTML 报告。
- pandas/NumPy 指标、确定性趋势评分、支撑阻力聚类和 Plotly 多子图。
- Windows 自动环境启动脚本、Linux 启动脚本、Dockerfile 和 Compose 配置。
- Plotly 默认平移、绘图形状及 `Ctrl+Z` 撤销；桌面和移动端图表宽度问题已修复。
- `.gitignore` 已覆盖环境变体、SQLite 旁路文件、日志、覆盖率和 Playwright 产物；新增 `.dockerignore`，排除虚拟环境、Git 元数据、测试及运行数据，保留镜像构建必需源码。

### 数据源与多市场

- 保留旧 `auto|stock|etf` 请求行为，并新增 `cn_stock`、`cn_etf`、`us_stock`、`us_index`、`global_future`。
- 美股通过 `stock_us_spot_em()` 代码表解析真实上游代码，不猜测市场前缀。
- 支持 `.IXIC/.NDX/.INX/.DJI` 美国指数，以及 `GC/SI/HG/CL/NG/OIL/XAU/XAG` 外盘期货。
- 期货使用连续参考序列，并单独获取短 TTL 快照；页面明确提示换月跳空风险。
- 元数据包含规范代码、上游代码、交易所、币种、时区、来源、能力、采集时间和单位。
- 中国市场红涨绿跌，海外市场绿涨红跌；缺少成交量时量能评分保持中性。

### 最近完成的 UI 工作

- “常用”列表不再写死任何证券，只读取浏览器 `localStorage`。
- 分析完成后可点击结果标题旁的 `☆/★` 添加或取消当前品种。
- 左键点击收藏项切换品种，悬停显示中文名称与市场。
- 右键收藏项显示自定义“取消收藏”菜单；点击空白、滚动、缩放、失焦或按 `Esc` 会关闭菜单。
- 首次使用显示“暂无收藏”，单个浏览器最多保存 30 项。
- K 线图新增黄色上三角“历史买入 Trigger”：只标收盘确认的做多 Trigger，不标趋势破位退出；悬停显示日期、Setup、规则质量分、收盘参考价及下一根执行提示。
- 波浪图新增 Top-1 候选连线：只连接已确认 Pivot，推动浪标注 `0～5`，ABC 标注起点与 `A～C`；悬停显示右侧确认滞后。
- Factor Snapshot 作为审计用高级数据保留，并在工作台默认折叠；API 与离线报告内容不变。
- 自动刷新会在相同证券、市场、周期、日期范围和图表上下文下保存并恢复用户手动缩放；手动分析或上下文变化不会复用旧范围。
- `AGENTS.md` 已更新信号标记、浪形情景、自动刷新视图保持和 UI 检查约束。

### 2026-09-01 浪形预测与信息展示优化

- 浪形候选升级为短/标准/宽三尺度竞争，补齐上涨与下跌推动浪、进行中 ABC，
  并明确区分进行中结构和已完成结构。
- 修复上涨 ABC 被错误投影为下跌、未完成第五浪失效位误用 Wave3 端点的问题；
  已完成结构现在只展示下一阶段反向观察区。
- 浪形情景增加收盘确认门槛，并在 K 线右侧绘制“情景 1：确认后延续”和
  “情景 2：尝试失败后失效”两条虚线折线；目标区、确认位和失效位继续使用
  水平基准，折线节点与横向距离明确声明仅为结构示意。
- 新增“情景 3：确认前震荡等待”和随情景 1 路径扩张的 ATR 半透明不确定性走廊。
- 新增浪形情景逐根无未来回放：确认只使用当根收盘，确认当根不能倒推使用盘中目标；
  同根目标/失效按失效保守处理。按浪型、方向、状态和尺度统计同类样本，已决样本
  少于 30 次时不展示概率。
- 修复生命周期过滤导致“有已确认 Pivot 却没有浪形连线和后市情景”的回归：结构候选
  现在始终保留；已完成结构穿过第一观察区后会推进至下一档 Fibonacci 观察区，而不是
  删除候选。新增真实浏览器 Plotly trace 检查，直接验证 Top-1、情景 1 和情景 2 均存在。
- 波浪百分比在界面中改称“结构匹配度（非概率）”，候选改为中文卡片展示。
- 工作台的“指标最新值”“公式与数据质量”“缓存与数据质量”和 Factor Snapshot
  默认折叠且可按需展开；离线报告同步折叠审计型内容。
- 浪形候选采用中文卡片显示浪型、方向、尺度、当前生命周期、确认门槛、目标区、
  失效规则和历史逐根回放统计；旧 `confidence` 字段仅为 API 兼容保留。
- 离线报告模板对缺少新增浪形历史统计字段的旧结果提供默认值，避免旧缓存或旧结构
  触发 Jinja `UndefinedError`。

### 2026-09-02 江恩算法图层与 UI 分组

- 新增 `app/gann/`，从分析区间内最近的右侧确认 Pivot 自动选择江恩锚点；开始日期只限定
  数据范围，不直接作为默认锚点。
- 新增 ATR 归一化 `2×1 / 1×1 / 1×2` 江恩角线、邻近价格分割、8～48 根 K 线
  时间观察窗、收盘确认位与结构失效位。角线不依赖屏幕缩放，周期不描述为精确日期预测。
- 新增江恩逐时点回放；只使用当时已确认锚点，同根目标与失效按失效优先，已决样本少于
  30 次时不展示概率；为控制分钟数据成本，验证使用最近最多 800 根 K 线。
- K 线图新增“波浪理论 / 江恩理论”独立按钮，可一次开关每套算法的全部 trace、shape 和
  annotation；默认波浪开启、江恩关闭，切换不会重置缩放，自动刷新保留开关状态。
- 修复江恩默认隐藏导致未来区间未参与 Plotly 初始取景、开启后趋势线与历史行情重叠而
  几乎不可见的问题；现在预留未来显示空间，并从最新 K 线向右高亮三条后续趋势段。
- 工作台和离线报告新增江恩结构说明、锚点确认时间、归一化尺度及历史验证结果。

### 2026-09-03 波浪与江恩跨周期可见性优化

- 波浪和江恩共用示意横轴：价格与回放周期保持原算法口径，横向显示至少占近期窗口的
  15%，解决多年日线下路径过短的问题。
- 图表首次加载默认聚焦近期 K 线并预留右侧空间：日线 220 根、周线 156 根、月线 120 根、
  分钟线 300 根；完整历史仍可平移查看。
- 算法按钮现在只切换整组 trace、shape 与 annotation，不再触发 autorange，因此用户手动
  缩放不会因切换波浪或江恩而丢失。
- 没有合法波浪候选时，K 线图内显示明确原因，不再以空白代替算法状态。
- 修复横轴聚焦近期数据后，价格纵轴仍受全历史极值影响、导致 GC 日线情景 1 被压扁的
  问题；默认价格轴现在覆盖近期高低点、Top-1 波浪结构及当前江恩价位。情景 1 改用
  高对比亮绿色粗实线与描边节点，避免和目标带、BOLL 线混淆。
- Playwright 检查新增桌面/移动端路径最小像素宽度、图层完整性和缩放保持断言。

### 2026-09-05 江恩结构 V2

- 同时保留上、下行右确认锚点候选；锚点需连续两根收盘穿越 ATR 缓冲区才结束生命周期，
  单根越界不会跳锚。候选分差较小时明确标为模糊，不参与方向确认。
- 修复扇形从最新收盘价重新起线的问题；三条角线现在全部由原始冻结锚点、固定归一化单位
  和经过根数计算，市场价格只用于判断角线的保持、降速、失守和收复事件。
- 新增三种有限尺度候选与可解释贴合评分，并只在角线、时间窗和价格分割同时重叠时生成
  时价共振观察区；观察区不解释为精确价格或日期预测。
- 历史回放按冻结锚点生命周期去重，分别统计方向确认、1×1 触及/守住、连续失守、目标
  结果以及 MFE/MAE。江恩背景以支持、冲突、中性或脆弱接入当前交易结论，不独立发单。

### 2026-09-05 江恩主锚晋升 V2.1

- 修复长期单边行情中旧锚只因未被反向跌破/突破而永久占据主锚的问题。同方向新 Pivot
  达到 1 ATR 摆幅并完成右侧三根确认后，因果晋升为当前主锚；旧锚保留为长期参考，
  不再参与当前扇线绘制。
- 主锚输出增加距今根数、晋升规则与长期参考锚；历史回放在下一同向主锚确认时结束旧生命周期，
  避免把晋升后的行情继续归到旧锚。
- 默认关闭的江恩与威科夫图层不再参与初始 Y 轴取景，远端理论投影不会压扁近期 K 线；
  图层开启时仍保持当前缩放，超出视窗部分可手动查看。

### 2026-09-03 TradingView 风格十字光标

- 工作台和离线报告的主 K 线区域新增横纵十字光标；纵线贯穿全部子图，横线覆盖价格区，
  底部坐标标签显示日期/时间，右侧坐标标签显示光标价格。
- 十字光标使用 Plotly 当前轴范围实时换算坐标，缩放和平移后仍保持准确；鼠标离开价格区或
  执行拖动时自动隐藏，不拦截 Plotly 的缩放、平移、悬停和绘图事件。
- 日线浏览器检查新增坐标标签内容、横纵线尺寸和实际显示状态断言。

### 2026-09-03 威科夫理论 V1

- 新增 `app/wyckoff/`，以仅使用当时可见数据的 60 根前置分位交易区间、ATR、价差和
  相对成交量识别 Spring、UTAD、SOS、SOW、LPS 与 LPSY，输出吸筹/派发候选及 Phase B～E。
- 输出收盘确认位、结构失效位和按区间宽度推导的目标观察区；路径横向距离仅作结构示意，
  不预测精确日期。历史验证逐根重建前缀，同根目标与失效按失效优先，少于 30 个已决样本
  不展示概率。
- 工作台和离线报告新增“威科夫理论”独立按钮，默认关闭，可完整切换区间、事件、两条情景线
  和 Phase 标注，并保持当前缩放。详情面板展示结构、事件链、匹配度、规则和历史样本状态。
- 成交量缺失或可靠比例不足 80% 时明确返回不可用，不将纯价格形态伪装成威科夫量价结构；
  因而 GC 连续参考序列当前会显示限制说明，而成交量完整的 A 股日线可正常生成候选。

### 2026-09-05 当前决策与历史 Trigger 改造

- 新增独立当前决策层。即使最新 K 线没有 Trigger，也会明确输出“等待、观察中、持仓管理、
  做多已确认、退出已确认或信号冲突”，并分别提供空仓计划、持仓计划、触发条件和取消观察/
  风控条件。
- 未触发时只展示条件计划，不伪造入场、止损与目标价格；严格 Trigger 确认后才自动展开下一根
  执行区、失效位、目标位、盈亏比和有效期。
- 同向并发 Trigger 选择规则质量分最高者；同一根 K 线出现做多与退出方向冲突时拒绝自动选边，
  明确显示暂缓执行。
- 同一 Setup 连续有效的生命周期只保留首次 Trigger，Setup 失效后才允许重新触发；当前结论、
  图表标记和策略回测统一使用该去重口径，重复触发只进入持仓管理状态，不自动加仓。
- “历史买入 Trigger”改名为“历史做多 Trigger（非实际成交）”；工作台将其归入 MA、BOLL、
  关键位所在的“图表叠加”并默认关闭，离线报告则通过 Plotly 技术线图例按需显示，不把它
  误列为波浪、江恩或威科夫理论。
- 首屏执行价格与历史统计改为折叠区；仅有可执行 Trigger 时自动展开，降低无信号状态下的占位。

### 2026-09-05 威科夫理论 V2

- 交易区间改为逐根因果冻结：使用前置数据建立边界，只有连续三根有效离开后才重建；单根
  Spring/UTAD 式越界不会让区间漂移。输出区间起点、持续根数、包含率、边界测试次数与
  ATR 宽度，图表矩形从真实冻结区间起点开始。
- 同时维护吸筹与派发两个竞争候选，加入 Phase A～E 事件状态机和 SC/BC、AR、ST、Test；
  LPS 必须位于 SOS 之后，LPSY 必须位于 SOW 之后，不再把孤立相似 K 线直接贴成后期事件。
- 事件分为收盘确认与后续行为确认；Spring/UTAD 可由 Test 确认，SOS/SOW 可由 LPS/LPSY
  或连续离开区间确认，界面事件链以 `✓` 标出已获跟随确认的事件。
- 结构匹配度改为显式评分构成：区间稳定、事件顺序、量价质量、后续确认、背景加分及冲突
  扣分；吸筹/派发分差不足 8 分时标记双候选接近，不作为独立方向确认。
- Phase C 使用 Spring/UTAD 极值确定失效，Phase D/E 优先使用 LPS/LPSY 或 SOS/SOW
  突破带；目标仍为冻结区间宽度的条件观察区，不预测精确日期。
- 历史回放按冻结交易区间和方向的生命周期去重，只在首次检测到 Phase C～E 时采样；分开
  报告结构确认率、确认后已决样本和目标先达率，并保留同根目标/失效时失效优先的保守规则。
- 当前决策新增威科夫背景：早期 Phase 和双候选接近时保持中性；Phase D/E 只作为严格
  Trigger 的支持或冲突证据，不越过 Trigger 独立发单。

## 已执行验证

- `ruff check .`：通过。
- `启动平台.bat --check`：通过，项目虚拟环境可用。
- `pytest -q`：`132 passed`，仅有一条第三方 `StarletteDeprecationWarning`。
- FastAPI 导入、`/health`、首页和静态资源：通过。
- Playwright + 本机 Edge：1440px 桌面及 390px 移动端无重叠、无横向溢出、无控制台错误。
- 收藏浏览器验证：空列表、添加、刷新持久化、中文 tooltip、右键菜单和取消收藏均通过。
- 量化 UI 离线 Playwright：1440px 与 390px 均无横向溢出或控制台错误；已覆盖
  Factor 默认折叠、波浪三情景文案、三套算法按钮独立显隐、切换时保持缩放、审计区展开
  和自动刷新缩放恢复。
- 2026-09-03 威科夫真实缓存 API 回归：`601628` 日线可生成派发候选、Phase、事件、交易
  区间、确认位、目标观察区与失效位；GC 因成交量不可靠按设计返回明确限制说明。
- 2026-09-03 威科夫 UI Playwright：桌面与移动端按钮、详情文案和 Plotly 整组图层切换通过，
  无横向溢出、控制台错误或算法按钮引发的坐标轴重置。
- 2026-09-05 量化 UI 离线 Playwright：桌面与 390px 移动端均通过；覆盖有 Trigger 时执行
  计划自动展开、无 Trigger 时条件计划可见且执行详情折叠、历史 Trigger 默认关闭/技术叠加切换，
  无横向溢出、控制台错误或图层切换引发的坐标轴重置。
- 2026-09-01 量化 UI 离线 Playwright 再次通过桌面与移动端检查，并新增审计区默认折叠/展开验证。
- 2026-09-01 真实缓存 API 回归：GC 返回 3 个浪形候选、600011 返回 2 个候选；
  不再出现有几十个确认 Pivot 但 `candidates=0` 的情况。
- `tests\ui_wave_check.py` 使用 GC 缓存行情在 1440px 与 390px 浏览器中直接读取
  Plotly trace，已确认 `浪形候选 Top-1`、`浪形情景 1`、`浪形情景 2` 均实际存在，
  且无横向溢出或控制台错误。
- 真实数据成功：`.IXIC`、`.NDX`、`.INX`、`GC`、`SI`、`CL`；期货快照成功。
- `docker compose config --quiet`：通过。

## 已知问题与外部限制

1. `AAPL`、`SPCX` 的真实验证未完成。本机代理访问 `72.push2.eastmoney.com` 时被远端断开；离线 mock 测试正常。不要把该代理错误判断为代码已失败或已成功。
2. 本轮当前终端未找到 `docker` 命令，可能未安装或未加入 PATH；此前只完成过 Compose 配置检查，镜像仍未完成实际构建验证。
3. FastAPI TestClient 产生第三方 Starlette/httpx 弃用警告，不影响当前 114 项测试结果；升级相关依赖前需确认兼容性。
4. AKShare 上游可能发生限流、代理干扰或字段变化。自动化 pytest 必须继续使用 mock，真实网络只作为单独冒烟测试。
5. 收藏保存在当前浏览器和站点源的 `localStorage`，不会跨浏览器、主机或无痕会话同步。
6. GC 连续参考序列当前没有可靠成交量，`volume_ratio_20` 等因子为 `NaN`。缺失量能在评分中保持中性，但现有 Setup 门槛会使 Trend Pullback 的缩量条件和 Breakout 的放量 Trigger 无法通过，GC 做多 Trigger 因此集中在 Support Reversal；调整前必须明确缺失量能的中性门槛并补多市场测试。

## 下一步计划

1. 设计缺失成交量的 Setup 中性规则，重点覆盖 `global_future`：不能把 `NaN` 当成放量确认，也不应无提示地永久封锁不依赖量能也可成立的结构。
2. 由用户重启平台后在常用浏览器中验收当前决策卡、条件计划、历史做多 Trigger 图层、浪形 Top-1 连线、三情景折线、
   ATR 不确定性走廊、审计区折叠、自动刷新缩放保持及原有收藏交互。
3. 调整 VPN/代理直连策略后，重新真实验证 `stock_us_spot_em()`、`AAPL` 和 `SPCX`，并生成美股离线报告。
4. 启动 Docker Desktop/daemon 后执行 `docker build` 和 `docker compose up`，再检查容器 `/health`。
5. 为收藏交互增加不依赖行情网络的自动 Playwright 测试，覆盖损坏的 localStorage 数据和 30 项上限。
6. 若继续扩展市场，先更新 `models.py` 能力校验和 provider mock，再接入路由、图表与报告。

## 继续工作命令

```powershell
cd C:\Users\MattLiu\Desktop\工具包\akshare-tech-analyzer
.\启动平台.bat --check
.\.venv\Scripts\python.exe -m ruff check .
.\.venv\Scripts\python.exe -m pytest -q
.\.venv\Scripts\python.exe -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

前端布局修改后，在服务运行期间执行：

```powershell
.\.venv\Scripts\python.exe tests\ui_layout_check.py --url http://127.0.0.1:8000
.\.venv\Scripts\python.exe tests\ui_quant_check.py --url http://127.0.0.1:8000
.\.venv\Scripts\python.exe tests\ui_wave_check.py --url http://127.0.0.1:8000
.\.venv\Scripts\python.exe tests\ui_gann_daily_check.py --url http://127.0.0.1:8000
```

所有结论仍须显示：仅为算法技术分析结果，不构成投资建议。
